# ─── Admin ──────────────────────────────────────────────────────────
# Plain-text admin password. Change for production.
ADMIN_PASSWORD="cambia-este-password"

# 32+ char secret to sign admin session cookies.
# Generate one with: openssl rand -base64 48
ADMIN_SESSION_SECRET="dev-only-secret-cambiar-en-prod-min-32-chars-XXXXXXXX"
